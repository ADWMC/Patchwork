# Runtime behavior is mounted by the Patchwork agent preset. Keeping this
# bundle patch empty lets `dsh plugin add` install the package dependency
# without injecting Patchwork into every agent in the profile.
[]
